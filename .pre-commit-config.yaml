repos:
  - repo: local
    hooks:
      - id: pdf-freshness
        name: Check that PDFs match their LaTeX sources
        entry: python3 scripts/pdf_freshness.py check
        language: system
        pass_filenames: false
        always_run: true
